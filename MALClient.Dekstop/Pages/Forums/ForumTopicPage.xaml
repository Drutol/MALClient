<Page
    x:Class="MALClient.Pages.Forums.ForumTopicPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MALClient.Pages.Forums"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xamlConverters="using:MALClient.Shared.XamlConverters"
    xmlns:userControls="using:MALClient.Shared.UserControls"
    xmlns:uwp="using:FontAwesome.UWP"
    xmlns:extensions="using:WinRTXamlToolkit.Controls.Extensions"
    xmlns:ui="using:Microsoft.Toolkit.Uwp.UI"
    xmlns:attachedProperties="using:MALClient.Shared.UserControls.AttachedProperties"
    mc:Ignorable="d" DataContext="{Binding Source={StaticResource Locator},Path=ForumsTopic}">

    <Page.Resources>
        <xamlConverters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
        <xamlConverters:BoolToVisibilityInvertedConverter x:Key="BoolToVisibilityInvertedConverter"/>
    </Page.Resources>
    
    <Grid Background="{ThemeResource BrushDeepBackground}">


        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
            <TextBlock Text="{Binding Title}" HorizontalAlignment="Center" FontSize="28" FontWeight="Medium" VerticalAlignment="Center" Margin="0,5"/>
        </StackPanel>
        <StackPanel Orientation="Horizontal" Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Center">
            <ItemsControl  ItemsSource="{Binding AvailablePages}" >
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <ItemsStackPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button Margin="2,0" Command="{Binding Source={StaticResource Locator},Path=ForumsBoard.LoadPageCommand}"
                            CommandParameter="{Binding Item1}" Padding="0">
                            <Grid Width="23" Height="25">
                                <Grid Background="{StaticResource SystemControlBackgroundAccentBrush}" Width="23" Height="25"
                                  Visibility="{Binding Item2,Converter={StaticResource BoolToVisibilityConverter}}"/>
                                <TextBlock Text="{Binding Item1}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </Grid>
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            <Button Margin="2,0">
                <Button.Flyout>
                    <Flyout x:Name="GotoPageFlyout">
                        <StackPanel>
                            <StackPanel Orientation="Horizontal" Margin="0,4">
                                <AutoSuggestBox x:Name="GotoPageTextBox" Header="Go to page" MinWidth="150" KeyDown="GotoInputOnKeyDown"  Text="{Binding GotoPageTextBind,Mode=TwoWay,UpdateSourceTrigger=PropertyChanged}" QueryIcon="Go" QuerySubmitted="GotoPageTextBox_OnQuerySubmitted"/>
                            </StackPanel>
                            <Grid Margin="0,4" HorizontalAlignment="Stretch">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition/>
                                    <ColumnDefinition/>
                                </Grid.ColumnDefinitions>
                                <Button Content="First" Margin="0,0,5,0" HorizontalAlignment="Stretch" Command="{Binding GotoFirstPageCommand}"/>
                                <Button Grid.Column="1" Content="Last" HorizontalAlignment="Stretch" Command="{Binding GotoLastPageCommand}"/>
                            </Grid>
                        </StackPanel>
                    </Flyout>
                </Button.Flyout>
                <SymbolIcon Symbol="Go" Height="25" Width="35"/>
            </Button>
        </StackPanel>
        <StackPanel  Grid.Row="1" Orientation="Horizontal">
            <!--<Button Content="Create new topic" Command="{Binding CreateNewTopicCommand}" Margin="0,0,5,0"
                Background="{ThemeResource BrushDeepBackground}" Visibility="{Binding NewTopicButtonVisibility,Converter={StaticResource BoolToVisibilityConverter}}"
                BorderThickness="2" BorderBrush="{StaticResource SystemControlBackgroundAccentBrush}"/>-->
        </StackPanel>
        <ScrollViewer Grid.Row="3" Background="{ThemeResource BrushAnimeItemInnerBackground}">
            <ItemsControl  ItemsSource="{Binding Messages}" HorizontalAlignment="Stretch">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="0,0,16,0">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto"/>
                                <RowDefinition Height="*"/>
                            </Grid.RowDefinitions>
                            <Grid Background="{ThemeResource BrushPivotHeaderBackground}" Padding="10" BorderThickness="4,0,0,0" BorderBrush="{StaticResource SystemControlBackgroundAccentBrush}">
                                <TextBlock Text="{Binding Data.CreateDate}"/>
                                <TextBlock Text="{Binding Data.MessageNumber}" FontWeight="SemiBold" HorizontalAlignment="Right"/>
                            </Grid>
                            <Grid Grid.Row="1" Background="{ThemeResource BrushAppBars}" Padding="4,0,0,0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <Grid>
                                    <StackPanel Margin="0,0,0,5">
                                        <Button Content="{Binding Data.Poster.MalUser.Name}" FontSize="20" Margin="0,5" Padding="0" Style="{StaticResource TextBlockButtonStyle}"/>
                                        <Image Margin="0,0,60,0" Source="{Binding Data.Poster.MalUser.ImgUrl}" extensions:ImageExtensions.FadeInOnLoaded="True" Stretch="UniformToFill"/>
                                        <TextBlock Text="{Binding Data.Poster.Status}"/>
                                        <TextBlock Text="{Binding Data.Poster.Joined}"/>
                                        <TextBlock Text="{Binding Data.Poster.Posts}"/>
                                    </StackPanel>
                                </Grid>
                                <Grid Grid.Column="1" VerticalAlignment="Stretch" MinHeight="230">
                                    <WebView attachedProperties:WebViewExtensions.Content="{Binding Data.HtmlContent}" ScriptNotify="WebView_OnScriptNotify"></WebView>
                                </Grid>
                            </Grid>

                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</Page>
