<UserControl
    x:Class="MALClient.Items.AnimeGridItem"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MALClient.Items"
    xmlns:converters="using:MALClient.XamlConverters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <UserControl.Resources>
        <converters:UrlToBitmapImageConverter x:Key="BitmapImageConverter"/>
    </UserControl.Resources>
    
    <Grid>
        <Image Stretch="UniformToFill" Source="{Binding Img,Converter={StaticResource BitmapImageConverter}}" 
               MaxWidth="{Binding MaxWidth}"/>
        <Grid Height="50" VerticalAlignment="Bottom">
            <Grid.Background>
                <SolidColorBrush Color="Black" Opacity=".75"/>
            </Grid.Background>
            <StackPanel>
                <TextBlock Text="{Binding Title}" Foreground="WhiteSmoke" TextTrimming="CharacterEllipsis"/>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
