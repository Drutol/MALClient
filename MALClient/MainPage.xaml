<Page
    x:Class="MALClient.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MALClient"
    xmlns:controls="using:MALClient.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xamlConverters="using:MALClient.XamlConverters"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" x:Name="Root">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid Width="Auto" HorizontalAlignment="Stretch" Background="{Binding ElementName=MainMenu , Path=Background}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="50"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button HorizontalAlignment="Left" FontWeight="Bold" FontFamily="Segoe MDL2 Assets" Content="&#xE700;" Width="45" Height="45" Background="Transparent" FontSize="20" Foreground="{StaticResource SystemControlBackgroundAccentBrush}"/>
            <TextBlock x:Name="CurrentStatus" Grid.Column="1" FontWeight="ExtraBold" Text="Home" VerticalAlignment="Center"/>
        </Grid>
        <SplitView x:Name="MainMenu" CompactPaneLength="50" OpenPaneLength="200" IsPaneOpen="False" DisplayMode="CompactOverlay" Grid.Row="0" Grid.RowSpan="2">
            <SplitView.Pane>
                <controls:HamburgerControl x:Name="HamburgerControl"/>
            </SplitView.Pane>
            <SplitView.Content>
                <Frame x:Name="MainContent"/>
            </SplitView.Content>
        </SplitView>
        
    </Grid>
</Page>
