<Page
    x:Class="MALClient.Pages.AnimeListPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MALClient.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Pin" Label="Pin to start (Mal)" Click="PinTileMal"></AppBarButton>
            <AppBarButton Icon="Pin" Label="Pin to start (KissAnime)" Click="PinTileKiss"></AppBarButton>
            <AppBarButton Icon="Filter" Label="Filter">
                <AppBarButton.Flyout>
                    <Flyout>
                        <ListView x:Name="StatusSelector" SelectionChanged="ChangeListStatus" SelectedIndex="0" SelectionMode="Single">
                            <ListView.Items>
                                <ListViewItem Content="Watching"></ListViewItem>
                                <ListViewItem Content="Completed"></ListViewItem>
                                <ListViewItem Content="On hold"></ListViewItem>
                                <ListViewItem Content="Dropped"></ListViewItem>
                                <ListViewItem Content="Plan to watch"></ListViewItem>
                                <ListViewItem Content="All"></ListViewItem>
                            </ListView.Items>
                        </ListView>
                    </Flyout>
                </AppBarButton.Flyout>
            </AppBarButton>
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid Grid.Row="0" HorizontalAlignment="Stretch" VerticalAlignment="Top">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="100"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="User" Text="Drutol" Grid.Column="0" Margin="5,5"/>
                <Button Click="FetchData"  HorizontalAlignment="Center" Margin="1,1" Grid.Column="1">Get Stuff</Button>
            </Grid>
            <ListView Grid.Row="1" x:Name="Animes" Margin="0,5,0,0" SelectionMode="Multiple">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    </Style>
                </ListView.ItemContainerStyle>
            </ListView>
        </Grid>
    </Grid>
</Page>
